<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Tawanda Munongo" %> | Tawanda Munongo</title>    
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content=" I spend most of my days learning about and building software systems. When I inevitably learn something new, I come here and write about it." />
    <meta name="keywords" content="Tawanda Munongo, Software Engineer, Developer, Blog, Portfolio" />
    <meta name="author" property="og:author" content="Tawanda Munongo" />
    <meta name="image" property="og:image" content="" />
    <meta property="og:image:height" content="1200" />
    <meta property="og:image:width" content="630" />
    <meta
      name="robots"
      content="index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1"
    />
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/favicon-16x16.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400..700&family=VT323&family=Press+Start+2P&display=swap" rel="stylesheet">

    <script
        defer
        data-domain="tawandamunongo.dev"
        src="https://analytics.tawandamunongo.dev/js/script.js"
      ></script>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= favicon_link_tag asset_path('favicon.ico') %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-[var(--color-base)] text-[var(--color-text-primary)] font-[var(--font-body)]" style="line-height: 1.8;">
    <%= render "shared/header" %>

    <main class="main-container w-screen md:max-w-[var(--container-max-width)] mx-auto px-4 py-8 prose">
      <%= yield %>
    </main>

    <%= render "shared/footer" %>
  </body>
</html>
